# 抖音下载页注册与验证

## 当前状态分析

前序会话已完成抖音主页作品下载功能的核心实现,所有功能文件均已就位:

| 文件 | 大小 | 状态 |
|------|------|------|
| `src/services/a_bogus.js` | 16313 字节 | ✅ 已创建(签名算法,ES5) |
| `src/services/douyin/douyinSigner.worker.js` | 1013 字节 | ✅ 已创建(Web Worker,纯 ES5) |
| `src/services/douyin/douyinSigner.ts` | 2801 字节 | ✅ 已创建(主线程签名管理器) |
| `src/services/douyin/douyinService.ts` | 8503 字节 | ✅ 已创建(API 调用+解析+下载) |
| `src/types/douyin.ts` | 1185 字节 | ✅ 已创建(类型定义) |
| `src/hooks/useDouyinDownload.ts` | - | ✅ 已创建(Cookie/查询/下载 hooks) |
| `src/pages/DouyinDownload.tsx` | - | ✅ 已创建(完整页面 UI) |
| `src-tauri/src/lib.rs` | - | ✅ 已改(media_type 参数 + 抖音 Referer) |

**剩余待完成**: 仅剩在 [appPages.tsx](file:///d:/trae_projects/fivedesigner/src/config/appPages.tsx) 注册页面入口 + 类型/构建验证。

## 关键发现

### appPages.tsx 现状
- `Download` 图标已在 L15 导入,被 BrowserManager(L185) 复用
- 数组末尾在 L188(`BrowserManager` 项的 `},`)后、L189(`]`)前
- 页面导入区在 L18-31

### 图标选择
`Download` 已被 BrowserManager 使用。为避免侧边栏两个相同图标,改用 `Video` 图标(语义契合抖音视频内容),需新增导入。

## 实施步骤

### 步骤 1: 注册 DouyinDownload 页面

**文件**: `src/config/appPages.tsx`

**改动 1 — 新增导入(L31 后追加)**:
```tsx
import DouyinDownload from '@/pages/DouyinDownload'
```

**改动 2 — 新增图标导入(L3-16 的 import 块内)**:
在 `Download,` 后追加 `Video,`(L15 后)。

**改动 3 — 数组末尾追加注册项(L188 后,即 BrowserManager 项之后)**:
```tsx
{
  navPath: '/douyin-download',
  routePaths: ['/douyin-download'],
  label: '抖音下载',
  description: '下载抖音主页图集和视频到素材库。',
  icon: Video,
  section: 'system',
  component: DouyinDownload,
},
```

### 步骤 2: 类型检查

运行 `npm run typecheck`,确认无 TypeScript 错误。重点关注:
- DouyinDownload.tsx 的导入与 hooks 类型
- useDouyinDownload.ts 的 React Query 类型
- douyinService.ts 的 invoke 调用类型

### 步骤 3: Rust 构建验证

运行 `cargo build`(在 src-tauri 目录),确认 lib.rs 的 `media_type` 参数与 Referer 分支编译通过。

## 假设与决策

1. **图标决策**: 使用 `Video` 而非复用 `Download`,避免侧边栏两个相同图标造成混淆。
2. **section 归属**: 归入 `system`(系统配置),与 BrowserManager、素材收集同区,符合"外部资源获取工具"定位。
3. **不修改 BrowserManager 的图标**: 保持最小改动,仅新增不重构。
4. **验证策略**: 类型检查 + cargo 构建双验证,确保前后端均无错误。

## 验证步骤

1. `npm run typecheck` 通过,无报错
2. `cargo build`(src-tauri 目录)通过,无报错
3. 启动 `npm run tauri:dev`,侧边栏"系统配置"区出现"抖音下载"入口
4. 点击进入页面,可看到完整 UI(标题、URL 输入、Cookie 按钮、作品列表区、进度区)
